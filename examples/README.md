# Offline fixture-backed analysis example

Run after `pip install -e ".[dev]"`:

```bash
python examples/run_fixture_analysis.py
```

This uses sample PGNs and a mock engine — no network or Stockfish required.
